spring:
  datasource:
    url: jdbc:mysql://localhost:3306/difman_mini?useSSL=false&autoReconnect=true&useUnicode=true&characterEncoding=utf-8
    username: root
    password: kgyqpt123^^
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL5Dialect
logging:
  level:
    org:
      hibernate: debug

